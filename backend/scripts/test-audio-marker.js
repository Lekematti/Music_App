require('dotenv').config(); const { createClient } = require('@supabase/supabase-js'); const c = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); (async ()=>{ try{ const key = 'test-audio-marker/.info.mp3'; const { data, error } = await c.storage.from('songs').upload(key, Buffer.from([0,1,2,3,4]), { contentType: 'audio/mpeg', upsert: true }); console.log('UPLOAD', error ? error.message : 'OK'); if (!error) { const dl = await c.storage.from('songs').download(key); if (dl.error) console.log('DL ERR', dl.error.message); else console.log('DL OK', Buffer.from(await dl.data.arrayBuffer()).slice(0,10)); } }catch(e){ console.error(e.message||e);} process.exit(0); })();